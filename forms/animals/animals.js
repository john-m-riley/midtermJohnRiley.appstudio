/*
let myAnimals = ["dog", "cat", "horse", "meerkat"]

let newAnimal = prompt("enter new animal to be added to the array")
let newAnimalLower = newAnimal.toLowerCase()
myAnimals.push(newAnimalLower)

alert(`The last animal is a/an ${myAnimals[myAnimals.length-1]}`)
*/
